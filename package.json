{
  "name": "notificacoes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate deploy",
    "prisma:setup": "npm run prisma:migrate && npm run prisma:generate",
    "nodemon": "nodemon ./bin/server.js",
    "build": "babel src -d dist --minified",
    "build:dev": "babel src -d dist --watch",
    "start": "npm run prisma:setup && node ./bin/server.js",
    "start:dev": "npm-run-all --parallel build:dev nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "dotenv": "^16",
    "express": "^4",
    "firebase-admin": "^13.6.0",
    "ioredis": "^5.3.2",
    "md5": "^2",
    "newrelic": "^11.6.0",
    "printf": "^0.6.1",
    "prisma": "^6.17.1",
    "pusher": "^5.2"
  },
  "devDependencies": {
    "@babel/cli": "^7",
    "@babel/preset-env": "^7",
    "nodemon": "^3",
    "npm-run-all": "^4"
  }
}
