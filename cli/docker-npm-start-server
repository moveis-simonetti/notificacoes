#!/bin/sh
set -e

RUNNER="start"

if [ "$NODE_ENV" = "development" ] || [ "$APP_ENV" = "dev" ]; then
  RUNNER="start:dev"
fi

echo "[docker-npm-start-server] NODE_ENV=$NODE_ENV APP_ENV=$APP_ENV"
echo "[docker-npm-start-server] Running: npm run $RUNNER"

exec npm run "$RUNNER"
